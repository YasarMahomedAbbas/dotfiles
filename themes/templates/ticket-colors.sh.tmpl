# Generated by theme-switch — do not edit. Active theme: ${theme_name}
# Sourced by ~/.local/bin/ticket-pill, which prints its own tmux pill (caps and
# colours included) because tmux cannot branch on a `#()`'s output and an empty
# pill still renders as a blob. The background is the same bg2 role tmux.conf.tmpl
# calls LIGHT_GRAY and wt-dev-colors.sh calls WT_PILL_BG, so the ticket pill, the
# worktree pill and the session pill are one row of identical shapes.
TICKET_PILL_BG='#${bg2}'
TICKET_NUM_FG='#${yellow}'
TICKET_TEXT_FG='#${fg1}'
