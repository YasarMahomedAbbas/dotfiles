# Generated by theme-switch — do not edit. Active theme: ${theme_name}
# source-d from ~/.config/hypr/hyprlock.conf. Variable names are prefixed
# (lock_*) so they don't collide with the palette role names during rendering.
$lock_bg0 = rgb(${bg0})
$lock_bg1 = rgb(${bg1})
$lock_fg0 = rgb(${fg0})
$lock_accent = rgb(${accent})
