# Generated by theme-switch — do not edit. Active theme: ${theme_name}
# Sourced by ~/.local/bin/wt-dev for both its status view and the tmux pill it
# prints. The pill background must match the other status-bar pills, so it comes
# from the same bg2 role tmux.conf.tmpl calls LIGHT_GRAY.
WT_OK='#${green}'
WT_WARN='#${yellow}'
WT_ERR='#${red}'
WT_ACCENT='#${accent}'
WT_FG='#${fg1}'
WT_MUTED='#${dim}'
WT_PILL_BG='#${bg2}'
