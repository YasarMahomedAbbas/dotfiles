ipc = on
splash = false

# Wallpaper is preloaded/set from hyprland.conf's exec-once (it runs through a shell
# that expands $HOME). hyprpaper's own config does NOT expand ~ or $HOME, so a static
# path here would not be portable across machines/usernames.
